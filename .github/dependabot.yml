# Dependency update PRs. Dependabot proposes the upgrades; the Security workflow
# (cargo-audit + cargo-deny) is what actually gates a vulnerable graph.
version: 2
updates:
  - package-ecosystem: cargo
    directory: "/"
    schedule:
      interval: weekly
      day: monday
    open-pull-requests-limit: 5
    groups:
      # The GPU/windowing stack moves in lockstep (vello ↔ wgpu ↔ winit);
      # upgrading them one at a time produces PRs that cannot compile.
      graphics-stack:
        patterns: ["vello", "wgpu", "winit", "peniko", "kurbo", "parley", "raw-window-handle"]
      dev-dependencies:
        dependency-type: development

  # Keep the SHA-pinned actions current — a pin is only safe if it is maintained.
  - package-ecosystem: github-actions
    directory: "/"
    schedule:
      interval: weekly
      day: monday
