[package]
name = "gallery"
version.workspace = true
edition.workspace = true
publish = false

[dependencies]
pebbles = { workspace = true, features = ["image-view", "file-dialogs", "markdown"] }

# Headless demo capture (GALLERY_CAPTURE=<dir>): vello WITH default features for
# the GPU `Renderer` + `util::RenderContext`, wgpu for the texture readback.
vello = { workspace = true, default-features = true }
wgpu.workspace = true
pollster.workspace = true

[lints]
workspace = true
