[package]
name = "gallery"
version.workspace = true
edition.workspace = true
publish = false

[dependencies]
pebbles = { workspace = true, features = ["image-view", "file-dialogs"] }

[lints]
workspace = true
